<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Project_Note" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/White</item>
        <!-- Chỉ định màu chữ chung cho tất cả các layout -->
        <item name="android:textColor">@color/Black</item>
        <item name="android:textColorHint">@color/Gray</item>
        <item name = "android:fontFamily">@font/nunitoregular</item>
    </style>

    <style name="Theme.Project_Note" parent="Base.Theme.Project_Note" />
</resources>